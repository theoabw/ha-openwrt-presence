# OpenWrt Presence integration package

This directory is the installable Home Assistant custom component. Copy the
whole `openwrt_presence` directory to
`/config/custom_components/openwrt_presence`; setup is then available through
Home Assistant's integration UI.

See the repository README for configuration, tracker enablement, state
semantics, privacy, and troubleshooting.
